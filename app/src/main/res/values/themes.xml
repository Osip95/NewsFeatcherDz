<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorToolBar">@color/primary</item>
        <item name="android:statusBarColor">@color/statusBarColor</item>
        <item name="colorBottomMenuBackground">@color/primary</item>
        <item name="itemPressedBottomMenuColor">@color/colorItem</item>
        <item name="itemNoPressedBottomMenuColor">@color/colorNoPressedItem</item>
        <item name="android:navigationBarColor">@color/statusBarColor</item>
        <item name="textColorTitle">@color/textColorTitle</item>

            </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
</resources>